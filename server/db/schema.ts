/**
 * Schema is managed via Supabase migrations.
 *
 * To apply the schema to your linked project:
 *   supabase db push
 *
 * To pull remote schema changes locally:
 *   supabase db pull
 *
 * Migration files live in: supabase/migrations/
 */
export function initSchema(): void {
  // No-op — schema is managed by Supabase migrations, not application code.
}
